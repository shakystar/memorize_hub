{
  "name": "@shakystar/memorize-hub-gateway-legacy",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "author": "shakystar",
  "description": "FROZEN pre-rebuild gateway — reference only, excluded from the pnpm workspace. Superseded by the workspace-aware clean rebuild in packages/gateway (see docs/protocol/ + docs/SoT/).",
  "type": "module",
  "bin": {
    "hub-gateway-admin": "dist/admin-cli.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "tsx scripts/e2e/run-gateway-e2e.ts",
    "lint": "eslint \"src/**/*.ts\" \"tests/**/*.ts\" \"scripts/**/*.ts\"",
    "check": "pnpm typecheck && pnpm lint && pnpm test"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^25.6.0",
    "@typescript-eslint/eslint-plugin": "^8.58.1",
    "@typescript-eslint/parser": "^8.58.1",
    "eslint": "^10.2.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
