{
  "name": "@shakystar/memorize-hub",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "author": "shakystar",
  "description": "memorize Hub — relay (transport) + gateway (control-plane) for memorize's cross-machine auto sync.",
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "check": "pnpm -r check"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
